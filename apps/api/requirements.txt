# --- Web framework (replaces Flask/Werkzeug/Jinja2/Flask-CORS — see
# AUDIT.md history for why the old Flask requirements.txt looked the
# way it did; this is the FastAPI equivalent) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# --- Settings / validation ---
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# --- Data sourcing & processing ---
fastf1>=3.3.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.31.0
requests-cache>=1.0.0

# --- Database ---
SQLAlchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9   # Postgres driver for Neon; SQLite needs no driver

# --- Auth (verifies the JWTs Auth.js issues on the frontend) ---
PyJWT>=2.8.0

# --- ML models ---
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# --- AI providers (optional "AI boost"/chat features) ---
openai>=1.0.0

# --- Caching (Upstash Redis in production, in-memory fallback locally —
# see cache/redis.py, ported unchanged) ---
redis>=5.0.0

# --- Reports (PDF/Excel export) ---
weasyprint>=60.0
openpyxl>=3.1.0
