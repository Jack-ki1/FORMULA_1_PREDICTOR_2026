export function TuningPanel({ weights, onChange }:any){ return <div className="card p-4"><div className="font-bold mb-2">Model Tuning</div>{Object.entries(weights||{}).slice(0,5).map(([k,v]:any)=><label key={k} className="flex gap-2 fs-11"><span className="w-32">{k}</span><input type="range" min={0} max={1} step={0.05} value={v as number} onChange={e=>onChange({...weights,[k]:parseFloat(e.target.value)})} /></label>)}</div>}
