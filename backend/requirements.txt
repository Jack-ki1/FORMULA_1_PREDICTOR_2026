# This file intentionally only includes the root requirements.
#
# It used to be a byte-for-byte duplicate of ../../requirements.txt, so every
# dependency change had to be made in two places or the two silently diverged
# (which is how `pytest` ended up missing while `pyproject.toml` declared it).
# The root file is now the single source of truth. See modify.md section 3.
-r ../requirements.txt
